name: Contract audit

on:
  pull_request:
    paths:
      - "contracts/**"
      - "docs/architecture/**"
      - "tools/contract-audit.rb"
      - "tools/sealed-fact-audit.mjs"
      - ".github/workflows/contract-audit.yml"
  push:
    branches: [main]
    paths:
      - "contracts/**"
      - "docs/architecture/**"
      - "tools/contract-audit.rb"
      - "tools/sealed-fact-audit.mjs"
      - ".github/workflows/contract-audit.yml"

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"
      - uses: actions/setup-node@v6
        with:
          node-version-file: .node-version
      - run: ruby tools/contract-audit.rb
      - run: node tools/sealed-fact-audit.mjs
