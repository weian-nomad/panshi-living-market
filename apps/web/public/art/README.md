# Generated art boundary

The observatory master is generated media and is intentionally excluded from
source control. Development may place an approved local copy at:

```text
five-seat-observatory-v1.webp
```

Production supplies the same approved digest through the asset release path.
The interface keeps a code-native astrolabe and dark material fallback, so a
missing illustration never hides status, controls, or text.
